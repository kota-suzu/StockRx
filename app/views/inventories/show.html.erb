<div class="container mx-auto px-4 py-8">
  <div class="mb-6 flex items-center justify-between">
    <div class="flex items-center gap-4">
      <%= link_to inventories_path, class: "text-gray-500" do %>
        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
        </svg>
      <% end %>
      <h1 class="text-2xl font-bold"><%= @inventory.name %></h1>
    </div>
    
    <div class="flex gap-4">
      <%= link_to "編集", edit_inventory_path(@inventory), class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
      <%= button_to "削除", inventory_path(@inventory), method: :delete, form: { data: { turbo_confirm: "本当に削除しますか？" } }, class: "bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded" %>
    </div>
  </div>
  
  <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-8">
    <div class="bg-white shadow-md rounded-lg p-6">
      <h2 class="text-xl font-semibold mb-4">在庫情報</h2>
      
      <div class="grid grid-cols-2 gap-4">
        <div>
          <p class="text-sm text-gray-500">商品名</p>
          <p class="font-medium"><%= @inventory.name %></p>
        </div>
        
        <div>
          <p class="text-sm text-gray-500">ステータス</p>
          <p><%= @inventory.status_badge %></p>
        </div>
        
        <div>
          <p class="text-sm text-gray-500">総在庫数</p>
          <p class="font-medium"><%= @inventory.quantity %></p>
        </div>
        
        <div>
          <p class="text-sm text-gray-500">単価</p>
          <p class="font-medium"><%= @inventory.formatted_price %></p>
        </div>
        
        <div>
          <p class="text-sm text-gray-500">在庫状態</p>
          <p><%= @inventory.alert_badge %></p>
        </div>
        
        <div>
          <p class="text-sm text-gray-500">最終更新</p>
          <p class="font-medium"><%= @inventory.updated_at_formatted %></p>
        </div>
      </div>
    </div>
    
    <div class="bg-white shadow-md rounded-lg p-6">
      <div class="flex items-center justify-between mb-4">
        <h2 class="text-xl font-semibold">ロット情報</h2>
        <button class="bg-green-500 hover:bg-green-700 text-white font-bold py-1 px-3 rounded text-sm">
          ロット追加
        </button>
      </div>
      
      <% if @inventory.batches.empty? %>
        <p class="text-gray-500 text-center py-4">登録されているロットがありません。</p>
      <% else %>
        <div class="overflow-x-auto">
          <table class="min-w-full divide-y divide-gray-200">
            <thead>
              <tr>
                <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">ロットコード</th>
                <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">数量</th>
                <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">有効期限</th>
                <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">状態</th>
              </tr>
            </thead>
            <tbody class="divide-y divide-gray-200">
              <% @inventory.batches.each do |batch| %>
                <tr class="<%= batch.expired? ? 'bg-red-50' : (batch.expiring_soon? ? 'bg-yellow-50' : '') %>">
                  <td class="px-4 py-2 whitespace-nowrap text-sm"><%= batch.lot_code %></td>
                  <td class="px-4 py-2 whitespace-nowrap text-sm"><%= batch.quantity %></td>
                  <td class="px-4 py-2 whitespace-nowrap text-sm"><%= batch.expires_on.present? ? l(batch.expires_on) : '設定なし' %></td>
                  <td class="px-4 py-2 whitespace-nowrap text-sm">
                    <% if batch.expired? %>
                      <span class="bg-red-200 text-red-800 px-2 py-1 rounded text-xs">期限切れ</span>
                    <% elsif batch.expiring_soon? %>
                      <span class="bg-yellow-200 text-yellow-800 px-2 py-1 rounded text-xs">期限間近</span>
                    <% else %>
                      <span class="bg-green-200 text-green-800 px-2 py-1 rounded text-xs">正常</span>
                    <% end %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      <% end %>
    </div>
  </div>
  
  <div class="bg-white shadow-md rounded-lg p-6">
    <h2 class="text-xl font-semibold mb-4">在庫履歴</h2>
    
    <div class="text-center py-8 text-gray-500">
      <p>この機能は準備中です。</p>
      <p class="text-sm mt-2">在庫の変動履歴を表示する予定です。</p>
    </div>
  </div>
</div> 